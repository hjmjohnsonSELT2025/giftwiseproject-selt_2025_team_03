<div class='recipient-page'>
    <div class='recipient-tabs'>
        <%= link_to "Manual",
            new_recipient_path,
            class: "tab #{action_name = 'new' ? 'active' : ''}"
        %>
        <%= link_to "Find by username", 
            find_recipient_path,
            class: "tab #{action_name == 'find' ? 'active' : ''}"
        %>
    </div>
</div>
<%# Allow maybe the inviter to 'invite' them to event, or 
# they can secretly add them to the event without the other knowing. %>


