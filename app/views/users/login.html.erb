<!-- Added elements to Login page for Theme from primary_layout -->
<%= render layout: 'shared/primary_layout' do %>
  <%= form_with model: @user, method: :post, class: "sign-in-form" do |f| %>
    <div class="decorative-line"></div>
    <h1 class="sign-in-heading">Sign in</h1>
    <div class="input-group">
      <%= label_tag :username, "Username", class: "input-label" %>
      <%= f.text_field :username, max_length: 20, placeholder: "", class: "input-field" %>
    </div>
    <div class="input-group">
      <%= label_tag :password, "Password", class: "input-label" %>
      <%= f.password_field :password, maxlength: 30, placeholder: "", class: "input-field" %>
    </div>
    <%= submit_tag "Login", class: "login-button" %>
    <div class="bottom-link">
      <p>"Don't have an account?" <%= link_to "Register", new_user_path %></p>
    </div>
  <% end %>
<% end %>
<%= javascript_import_module_tag "controllers/users/login"%>